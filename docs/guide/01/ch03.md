---
stars: 1
readTime: 2
readWithAI: /ai-reading/01-ch03.6db2f3b43e94.json
---
# 第 3 章 数据库 · 软件的记忆

<ReadWithAI />

<figure class="book-figure">
<svg viewBox="0 0 720 258" role="img" data-caption="图 3 · 表、行、列：一个极其严谨的 Excel" aria-label="示意图：数据库的一张表像 Excel 工作表，一行是一条记录，一列是一个属性，和数据库说话的语言是 SQL。"> <text class="tk" x="60" y="42">表 Table · 一个极其严谨的 Excel 工作表</text> <rect class="s" x="60" y="60" width="420" height="140" rx="2"/> <line class="s s2" x1="60" y1="92" x2="480" y2="92"/> <line class="s s3" x1="60" y1="128" x2="480" y2="128"/> <line class="s s3" x1="60" y1="164" x2="480" y2="164"/> <line class="s s3" x1="165" y1="60" x2="165" y2="200"/> <line class="s s3" x1="270" y1="60" x2="270" y2="200"/> <line class="s s3" x1="375" y1="60" x2="375" y2="200"/> <text class="tm" x="112" y="81" text-anchor="middle">id</text> <text class="tm" x="217" y="81" text-anchor="middle">email</text> <text class="ts" x="322" y="81" text-anchor="middle">注册时间</text> <text class="ts" x="427" y="81" text-anchor="middle">状态</text> <line class="s s3" x1="88" y1="112" x2="138" y2="112"/> <line class="s s3" x1="185" y1="112" x2="250" y2="112"/> <line class="s s3" x1="290" y1="112" x2="355" y2="112"/> <line class="s s3" x1="395" y1="112" x2="440" y2="112"/> <line class="s s3" x1="88" y1="148" x2="138" y2="148"/> <line class="s s3" x1="185" y1="148" x2="250" y2="148"/> <line class="s s3" x1="290" y1="148" x2="355" y2="148"/> <line class="s s3" x1="395" y1="148" x2="440" y2="148"/> <line class="s s3" x1="88" y1="184" x2="138" y2="184"/> <line class="s s3" x1="185" y1="184" x2="250" y2="184"/> <line class="s s3" x1="290" y1="184" x2="355" y2="184"/> <line class="s s3" x1="395" y1="184" x2="440" y2="184"/> <rect class="hl" x="60" y="128" width="420" height="36"/> <line class="s s2" x1="484" y1="146" x2="520" y2="146"/> <text class="t" x="528" y="150">行 Row · 一条记录，比如一个用户</text> <rect class="hl" x="165" y="60" width="105" height="140"/> <line class="s s2" x1="217" y1="200" x2="217" y2="222"/> <text class="t" x="217" y="240" text-anchor="middle">列 Column · 一个属性</text> <text class="tm" x="528" y="184">SELECT * FROM users</text> <text class="ts" x="528" y="206">和它说话的语言叫 SQL</text> <text class="ts" x="528" y="226">核心只有查 · 增 · 改 · 删</text> <text class="mk" x="700" y="250" text-anchor="end">stickmancharles.com · CC BY-NC-ND 4.0</text> </svg>
<figcaption>图 3 · 表、行、列：一个极其严谨的 Excel</figcaption>
</figure>

程序有个天生的毛病，一关掉，脑子里的东西就全没了。可用户第二天回来，凭什么你还记得他是谁、他传过什么文件、他有哪些凭证？靠的是<strong>数据库（Database）</strong>，一个专门负责记住的软件。这些东西从来不存在程序里，程序只在需要时去数据库里取。

最快的理解方式是把数据库想成一个极其严谨的 Excel。一张<strong>表（Table）</strong>对应 Excel 里的一个工作表。一<strong>行（Row）</strong>是一条记录，比如一个用户。一<strong>列（Column）</strong>是一个属性，比如邮箱、注册时间。我们用的 <strong>Supabase</strong>，底下就是世界上最受欢迎的开源数据库 <strong>Postgres</strong>。

和数据库说话有专门的语言，叫 <strong>SQL</strong>。别被吓到，它的核心只有四个动词，查（SELECT）、增（INSERT）、改（UPDATE）、删（DELETE）。比如这一句。

```sql
SELECT * FROM users WHERE email = 'dad@example.com';
```

意思是从 users 表里，把邮箱等于这个的行都找出来。你看 Claude Code 写的 SQL，八成都是这四个动词的变体。

那为什么不直接用 Excel 或者一堆文件来存？数据库解决了四个 Excel 搞不定的问题。一千个人<strong>同时</strong>读写不打架。在百万行里瞬间查到你要的那一条。精确控制谁能看哪些行、谁能改哪些列。断电、崩溃也不丢数据。至于 Supabase 是什么，它帮你把 Postgres 托管好，再送你登录系统（Auth）、文件存储和现成的 API，所以 Claude Code 三句话不离它。

顺带认识一个你常会见到的词，<strong>迁移（Migration）</strong>。每次修改表结构，加一列，建一张表，都写成一个脚本保存下来，让数据库的每一步变化都有据可查、可以重放，相当于给数据库结构做版本记录。

::: info 关键领悟
你产品里一切属于用户的东西，几乎都住在数据库里。设计表结构的过程，就是想清楚我的产品到底要记住什么。
:::

::: tip 试一试
打开 Supabase 后台的 Table Editor，随便点开一张表，亲手把表、行、列各自指认一遍。
:::
