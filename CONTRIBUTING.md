# 参与这套书

这个仓库装着《AI 时代的编程指南》的书稿和 books.stickmancharles.com 的全部源码。欢迎你来改它。第五册第 13 章说过，PR 是世界通用的协作礼仪，这里就是练习它的地方。

## 欢迎哪些贡献

- 错别字、病句、标点问题
- 事实错误、过时的技术细节（工具界面变了、服务改名了）
- 死链、构建问题、站点代码的修复与改进
- 术语表和速查卡的补充

想做大改动（重写一章、新增一章、调整结构），先开一个 issue 说明想法，谈拢再动手，免得白写。

翻译请先来信，见网站的[许可页](https://books.stickmancharles.com/license)，通常会同意。

## 动手之前，先懂一条规矩

**单一真相源。**书稿只住在 `books/`，站点章节页由 `scripts/assemble.mjs` 生成。改书的流程是

1. 改 `books/` 下对应的书稿
2. 跑 `npm run assemble`
3. 永远不要手改 `docs/guide/` 下的生成文件，它们下次装配就会被覆盖

站点其他页面（`docs/start/`、`docs/reference/`、`docs/projects/` 等）是手写的，直接改。

## 本地跑起来

```bash
npm install
npm run docs:dev     # 本地预览
npm run docs:build   # 构建，会校验全站每一条内链
```

提 PR 前先跑一次 `npm run docs:build`，红灯的 PR 不会被合并，这是第五册第 8 章讲的门禁，本仓库自己也守。

## 书稿的格式约定

改书稿时请保持这几样原样，装配脚本靠它们工作。

- 章标题格式 `# 第 N 章 主标题 · 副标题`，侧边栏短标题取"·"前的部分
- `**关键领悟**：`、`**试一试**：`、`**出事时想起我**：` 这类行首标记（含全角冒号）会被转成页面卡片
- 第五册用 `::: info` / `::: tip` / `::: warning` 容器，格式照旧
- `# 附：本册新词`、`# 写在最后` 这些标题是装配脚本的匹配键

行文上，这套书刻意不用破折号和提示性冒号，判断从正面说。拿不准就模仿邻近段落的写法。

## PR 流程

1. Fork，开分支，`fix-` 开头修问题，`feat-` 开头加东西
2. 小步提交，每个 commit 说清这次改了什么
3. PR 描述里写清改了什么、为什么，涉及事实修正的给出依据

## 许可

提交贡献即表示你同意，代码部分按 MIT（`LICENSE`）授权，正文部分并入 CC BY-NC-ND 4.0（`LICENSE-CONTENT.md`）的作品。你的署名会留在 Git 历史里。正文类改动是否合并由维护者判断，标准只有一个，让书更准、更好读。

## 行为准则

互相学习，就这一条。
